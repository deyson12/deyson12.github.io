import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ventas7lunas.app',
  appName: 'Ventas7Lunas',
  webDir: 'dist/ventas-7-lunas/browser'
};

export default config;
