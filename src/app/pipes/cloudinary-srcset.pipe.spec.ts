import { CloudinarySrcsetPipe } from './cloudinary-srcset.pipe';

describe('CloudinarySrcsetPipe', () => {
  it('create an instance', () => {
    const pipe = new CloudinarySrcsetPipe();
    expect(pipe).toBeTruthy();
  });
});
