import { OptimizeCloudinaryPipe } from './optimize-cloudinary.pipe';

describe('OptimizeCloudinaryPipe', () => {
  it('create an instance', () => {
    const pipe = new OptimizeCloudinaryPipe();
    expect(pipe).toBeTruthy();
  });
});
