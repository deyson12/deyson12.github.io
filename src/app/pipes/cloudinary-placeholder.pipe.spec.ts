import { CloudinaryPlaceholderPipe } from './cloudinary-placeholder.pipe';

describe('CloudinaryPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new CloudinaryPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
